#ifndef CODEC_H
#define CODEC_H
/* Angle-include target; pulls in a nested quote-include relative to itself. */
#include "codec/freq.h"
#define CODEC_CAMPBELL 14085
struct Ration { int count; short flags; };
#endif
