# npm 11's peer-set resolver crashes on Vitest's optional peer graph
# ("Cannot read properties of null (reading 'edgesOut')"). All peers this
# project needs are declared explicitly, so legacy resolution is safe.
legacy-peer-deps=true
